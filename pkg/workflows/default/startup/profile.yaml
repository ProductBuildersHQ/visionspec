name: startup
description: "Lightweight configuration for pre-PMF startups. Only PRD required."

spec_config:
  # Source specs: only PRD required
  prd:
    required: true
    category: source
  mrd:
    required: false
    category: source
    template: {from: enterprise}
    rubric: {from: enterprise}
  uxd:
    required: false
    category: source
    description: "Lightweight UX design for MVP"

  narrative-1p:
    required: false
    category: gtm
    template: {from: enterprise}
    rubric: {from: enterprise}
  # Technical specs: optional until architecture stabilizes
  trd:
    required: false
    category: technical
    description: "Lightweight technical design for MVP"
  ird:
    required: false
    category: technical
    template: {from: enterprise}
    rubric: {from: enterprise}
